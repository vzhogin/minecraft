#!groovy
// Save data server
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'master'
        }
    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
    }
        stage("Save data on server") {
            steps {
                sh 'ssh minecraft@192.168.1.9 \'screen -r minecraft -X stuff 'save-all^m'\''
            }
        }
    }
